logging: "/var/log/backup.log"
#method can be aws, scp or folder
#folder needs to be given for folder option
#folder and key file will be required for aws option
backup_method: aws
backup_folder: /tmp/backups
aws:
 bucket: localdotnet
 lockfile: /tmp/.cliinstalled
files:
# websrv_files:
 - /etc/apache2/apache2.conf
# vhost_files:
# - /etc/apache2/site-available/wp.conf
# my:
 - /etc/mysql/my.cnf
# php:
 - /etc/php/7.4/apache2/php.ini
folders:
 - /var/www/html/
websrv_cmdfull:
 a2ensite wp.conf
 systemctl apache2 restart
websrv_cmdwp:
 a2ensite wp.conf
 systemctl apache2 reload
